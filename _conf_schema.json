{
    "url": {
        "description": "API URL",
        "type": "string",
        "hint": "请输入API的URL地址",
        "default": "https://api.siliconflow.cn/v1"
    },
    "apikey": {
        "description": "API Key",
        "type": "string",
        "hint": "请输入API的密钥"
    },
    "name": {
        "description": "模型名称",
        "type": "string",
        "hint": "请输入模型的名称，如：FunAudioLLM/CosyVoice2-0.5B",
        "default": "FunAudioLLM/CosyVoice2-0.5B"
    },
    "voice": {
        "description": "音色配置",
        "type": "string",
        "hint": "系统预置音色：模型名:音色名（如：FunAudioLLM/CosyVoice2-0.5B:alex）；自定义音色：speech:音色名:ID:哈希值"
    },
    "group_access_mode": {
        "description": "会话访问控制模式",
        "type": "string",
        "options": ["不限制", "白名单", "黑名单"],
        "hint": "不限制=不限制；白名单=仅名单内启用；黑名单=名单内禁用",
        "default": "不限制"
    },
    "group_access_list": {
        "description": "会话访问控制列表（群号或QQ号）",
        "type": "list",
        "hint": "与上面的模式配合使用；填群号或QQ号；示例：[\"1234567890\"]",
        "default": []
    },
    "skip_tts_keywords": {
        "description": "跳过TTS的关键词列表",
        "type": "list",
        "hint": "当回复包含这些关键词时，将跳过语音转换，直接回复文字（不区分大小写）",
        "default": ["astrbot", "llm", "http", "www.", ".com", "reset", ".cn", "链接", "网址", "入群", "退群", "图片", "涩图", "语音", "音色", "错误类型", "tts", "转换", "新对话", "服务提供商", "列表"]
    },
    "only_llm_tts": {
        "description": "仅对AI模型回复进行TTS",
        "type": "bool",
        "hint": "开启后，只对由大模型生成的回复进行语音合成；插件或人工触发的非LLM文本将不做TTS",
        "default": true
    },
    "tts_probability": {
        "description": "TTS转换概率",
        "type": "int",
        "hint": "设置语音转换的概率，范围0-100。100表示每次都转换，50表示50%概率转换，0表示从不转换",
        "default": 100
    },
    "speed": {
        "description": "音频播放速度",
        "type": "float",
        "hint": "控制音频播放速度，float类型，默认值是1.0，可选范围是[0.25,4.0]",
        "default": 1.0
    },
    "gain": {
        "description": "音频增益",
        "type": "float",
        "hint": "音频增益，单位dB，控制音频声音大小，float类型，默认值是0.0，可选范围是[-10,10]",
        "default": 0.0
    },
    "max_tts_chars": {
        "description": "TTS 最大字符数",
        "type": "int",
        "hint": "当文本长度超过该值时，将跳过TTS转换。设置0或负数表示不限制。",
        "default": 0
    },
    "global_enabled": {
        "description": "全局TTS状态保存",
        "type": "bool",
        "hint": "控制TTS功能的全局开关状态，开启后则不会因为重启框架或重载插件而改变TTS功能开关状态",
        "default": true
    },
    "reference_mode": {
        "description": "参考模式（语音 + 原文本）",
        "type": "bool",
        "hint": "开启后，触发TTS时会先发送语音，再附上原文本内容",
        "default": false
    },
    "debug_tts_input": {
        "description": "调试：显示发送到TTS的完整文本",
        "type": "bool",
        "hint": "开启后，将先发送语音，再发送用于TTS的完整文本,非必要不开启。",
        "default": false
    },
    "max_saved_audios": {
        "description": "最大保存音频数量（0=不限制）",
        "type": "int",
        "hint": "保存到 data/astrbot_plugin_vits/tts/ 的 .wav 文件数量上限。超过后自动删除最早的。设置为 0 表示不限制。",
        "default": 5
    },
    "read_brackets": {
        "description": "是否朗读括号中的内容",
        "type": "bool",
        "hint": "关闭后，将删除圆括号和方括号中的内容再送去TTS（如旁白、注释等）",
        "default": true
    },
    "filter_symbols_enabled": {
        "description": "是否过滤指定符号",
        "type": "bool",
        "hint": "开启后，将从文本中移除下面列表中配置的符号字符再送去TTS",
        "default": false
    },
    "filter_symbols": {
        "description": "需要过滤掉不朗读的符号字符",
        "type": "list",
        "hint": "这些符号会在TTS前被删除，例如默认值中的 + - = / 等，用户可自行增删",
        "default": ["+", "-", "=", "/"]
    },
    "enable_llm_tool": {
        "description": "允许大模型主动调用TTS工具",
        "type": "bool",
        "hint": "开启后，大模型作为Agent可以通过工具自主决定何时发送语音（需要大模型支持Function Calling）",
        "default": true
    },
    "enable_llm_response": {
        "description": "工具调用时是否保留大模型文本",
        "type": "bool",
        "hint": "关闭后，大模型主动调用语音时将只发送语音，不发送文字。开启则语音文字一起发送。",
        "default": false
    }
}
